
export enum LectureType {
  LECTURE = 1,
  QUIZ,
}

export enum CourseStatus {
  REVIEW_INIT = 0,
  REVIEW_PENDING,
  REVIEW_VERIFY,
}

export enum AssetType {
  RESOURCE = 1,
  WATCH,
}

export enum UploadType {
  UPLOAD_ASSET = 1,
  UPLOAD_ARTICLE,
  REMOVE_ASSET,
}
